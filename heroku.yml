setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
      as: DATABASE

build:
  docker:
    web: Dockerfile  # Specify your Dockerfile for the web service

run:
  web: uvicorn app.main:app --host 0.0.0.0 --port ${PORT}  # Update based on your app